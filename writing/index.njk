---
layout: page-layout.njk
title: justGoscha — Writing & Media
css: /style/writing.css
---

<div class="masthead">
  <h1>Writing &amp; Media</h1>
  <hr class="rule-double">
  <div class="subtitle">Substacks &middot; Videos &middot; Archive</div>
</div>

<div class="editorial-grid">

  {# ── Row 1: Inspired Animator (2 cols) + YouTube Videos (2 cols) ── #}
  {% set primarySub = writing.substacks[0] %}
  <div class="pub-box span-2">
    <div class="pub-header">
      <a class="pub-name" href="{{ primarySub.homeUrl }}" target="_blank" rel="noopener">{{ primarySub.name }}</a>
      <span class="pub-badge badge-substack">Substack</span>
    </div>
    {% if primarySub.posts.length %}
    <ul class="article-list">
      {% for post in primarySub.posts.slice(0, 6) %}
      <li{% if loop.first %} class="featured"{% endif %}>
        <a href="{{ post.link }}" target="_blank" rel="noopener">{{ post.title }}</a>
        {% if loop.first and post.description %}
        <div class="excerpt">{{ post.description }}</div>
        {% endif %}
        <div class="article-date">{{ post.pubDate | date }}</div>
      </li>
      {% endfor %}
    </ul>
    {% else %}
    <p class="no-posts">No posts available.</p>
    {% endif %}
    <a class="read-all" href="{{ primarySub.homeUrl }}" target="_blank" rel="noopener">Read all &rarr;</a>
  </div>

  <div class="pub-box span-2">
    <div class="pub-header">
      <a class="pub-name" href="{{ writing.youtube.homeUrl }}" target="_blank" rel="noopener">YouTube</a>
      <span class="pub-badge badge-video">Video</span>
    </div>
    {% if writing.youtube.videos.length %}
    <ul class="video-list">
      {% for vid in writing.youtube.videos.slice(0, 5) %}
      <li>
        <img class="video-thumb" src="{{ vid.thumbnail }}" alt="{{ vid.title }}" loading="lazy">
        <div class="video-info">
          <a href="{{ vid.link }}" target="_blank" rel="noopener">{{ vid.title }}</a>
          <div class="article-date">{{ vid.pubDate | date }}</div>
        </div>
      </li>
      {% endfor %}
    </ul>
    {% else %}
    <p class="no-posts">No videos available.</p>
    {% endif %}
    <a class="read-all" href="{{ writing.youtube.homeUrl }}" target="_blank" rel="noopener">Watch all &rarr;</a>
  </div>

  {# ── Row 2: Pain Meds (2 cols) + ZenMemo (1 col) + Technograf (1 col) ── #}
  {% set secondSub = writing.substacks[1] %}
  <div class="pub-box span-2">
    <div class="pub-header">
      <a class="pub-name" href="{{ secondSub.homeUrl }}" target="_blank" rel="noopener">{{ secondSub.name }}</a>
      <span class="pub-badge badge-substack">Substack</span>
    </div>
    {% if secondSub.posts.length %}
    <ul class="article-list">
      {% for post in secondSub.posts.slice(0, 6) %}
      <li{% if loop.first %} class="featured"{% endif %}>
        <a href="{{ post.link }}" target="_blank" rel="noopener">{{ post.title }}</a>
        {% if loop.first and post.description %}
        <div class="excerpt">{{ post.description }}</div>
        {% endif %}
        <div class="article-date">{{ post.pubDate | date }}</div>
      </li>
      {% endfor %}
    </ul>
    {% else %}
    <p class="no-posts">No posts available.</p>
    {% endif %}
    <a class="read-all" href="{{ secondSub.homeUrl }}" target="_blank" rel="noopener">Read all &rarr;</a>
  </div>

  {% for sub in writing.substacks.slice(2, 4) %}
  <div class="pub-box">
    <div class="pub-header">
      <a class="pub-name" href="{{ sub.homeUrl }}" target="_blank" rel="noopener">{{ sub.name }}</a>
      <span class="pub-badge badge-substack">Substack</span>
    </div>
    {% if sub.posts.length %}
    <ul class="article-list">
      {% for post in sub.posts.slice(0, 5) %}
      <li>
        <a href="{{ post.link }}" target="_blank" rel="noopener">{{ post.title }}</a>
        <div class="article-date">{{ post.pubDate | date }}</div>
      </li>
      {% endfor %}
    </ul>
    {% else %}
    <p class="no-posts">No posts available.</p>
    {% endif %}
    <a class="read-all" href="{{ sub.homeUrl }}" target="_blank" rel="noopener">Read all &rarr;</a>
  </div>
  {% endfor %}

  {# ── Row 3: YouTube Shorts ── #}
  {% if writing.youtube.shorts.length %}
  <div class="pub-box span-4">
    <div class="pub-header">
      <a class="pub-name" href="{{ writing.youtube.homeUrl }}" target="_blank" rel="noopener">Shorts</a>
      <span class="pub-badge badge-video">YouTube</span>
    </div>
    <div class="shorts-scroll">
      {% for short in writing.youtube.shorts %}
      <a class="short-card" href="{{ short.link }}" target="_blank" rel="noopener">
        <img src="{{ short.thumbnail }}" alt="{{ short.title }}" loading="lazy">
        <div class="short-title">{{ short.title }}</div>
      </a>
      {% endfor %}
    </div>
  </div>
  {% endif %}

  {# ── Row 4: Around the Web ── #}
  <div class="pub-box span-4 social-box">
    <div class="pub-header">
      <span class="pub-name">Around the Web</span>
      <span class="pub-badge badge-social">Links</span>
    </div>
    <div class="social-grid">
      <a class="social-link" href="https://linkedin.com/in/justgoscha" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-4 0v7h-4v-7a6 6 0 0 1 6-6z"/><rect x="2" y="9" width="4" height="12"/><circle cx="4" cy="4" r="2"/></svg>
        <span>LinkedIn</span>
      </a>
      <a class="social-link" href="https://instagram.com/justgoscha" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"/><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"/><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/></svg>
        <span>Instagram</span>
      </a>
      <a class="social-link" href="https://tiktok.com/@justgoscha" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>
        <span>TikTok</span>
      </a>
      <a class="social-link" href="https://medium.com/@justgoscha" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>
        <span>Medium</span>
      </a>
      <a class="social-link" href="https://substack.com/@justgoscha" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><line x1="4" y1="5" x2="20" y2="5"/><line x1="4" y1="9" x2="20" y2="9"/><path d="M4 13l8 6 8-6"/></svg>
        <span>Substack</span>
      </a>
      <a class="social-link" href="https://bsky.app/profile/justgoscha.bsky.social" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3c-3 3-8 6.5-8 11a5 5 0 0 0 8 1"/><path d="M12 3c3 3 8 6.5 8 11a5 5 0 0 1-8 1"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
        <span>Bluesky</span>
      </a>
      <a class="social-link" href="https://youtube.com/@justgoscha" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M22.54 6.42a2.78 2.78 0 0 0-1.94-2C18.88 4 12 4 12 4s-6.88 0-8.6.46a2.78 2.78 0 0 0-1.94 2A29 29 0 0 0 1 11.75a29 29 0 0 0 .46 5.33A2.78 2.78 0 0 0 3.4 19.1c1.72.46 8.6.46 8.6.46s6.88 0 8.6-.46a2.78 2.78 0 0 0 1.94-2 29 29 0 0 0 .46-5.25 29 29 0 0 0-.46-5.43z"/><polygon points="9.75 15.02 15.5 11.75 9.75 8.48 9.75 15.02"/></svg>
        <span>YouTube</span>
      </a>
      <a class="social-link" href="https://twitter.com/justgoscha" target="_blank" rel="noopener">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M23 3a10.9 10.9 0 0 1-3.14 1.53 4.48 4.48 0 0 0-7.86 3v1A10.66 10.66 0 0 1 3 4s-4 9 5 13a11.64 11.64 0 0 1-7 2c9 5 20 0 20-11.5a4.5 4.5 0 0 0-.08-.83A7.72 7.72 0 0 0 23 3z"/></svg>
        <span>Twitter</span>
      </a>
    </div>
  </div>

  {# ── Row 5: Old Blog Archive ── #}
  <div class="pub-box span-4 archive-box">
    <div class="pub-header">
      <a class="pub-name" href="{{ writing.oldBlog.homeUrl }}" target="_blank" rel="noopener">{{ writing.oldBlog.name }}</a>
      <span class="pub-badge badge-archive">Archive</span>
    </div>
    {% if writing.oldBlog.posts.length %}
    <ul class="archive-list">
      {% for post in writing.oldBlog.posts %}
      <li>
        <a href="{{ post.link }}" target="_blank" rel="noopener">{{ post.title }}</a>
        <div class="article-date">{{ post.pubDate | date }}</div>
      </li>
      {% endfor %}
    </ul>
    {% else %}
    <p class="no-posts">No archive posts available.</p>
    {% endif %}
    <a class="read-all" href="{{ writing.oldBlog.homeUrl }}" target="_blank" rel="noopener">Visit archive &rarr;</a>
  </div>

</div>
